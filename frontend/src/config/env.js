const env = {
    app_name: import.meta.env.APP_NAME || "Rank Eats",
    vite_api_url: import.meta.env.VITE_API_URL || "http://localhost:3000",
    site_url: import.meta.env.SITE_URL || "http://localhost:3000",

};

export default env